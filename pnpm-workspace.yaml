allowBuilds:
  esbuild: true   # Allow esbuild to run its build script
  msw: true       # Allow msw
  sharp: true     # Allow sharp
  # some-other-package: false  # Explicitly block

overrides:
  esbuild@>=0.17.0 <0.28.1: ^0.28.1
  postcss@<8.5.10: ^8.5.15